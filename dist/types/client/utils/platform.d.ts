export declare const isMac: boolean;
export declare const MOD_KEY: string;
/** Platform-idiomatic modifier chord label: `modChord('K')` → "⌘K" on Mac, "Ctrl+K" elsewhere. */
export declare function modChord(key: string): string;
