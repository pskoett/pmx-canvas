import type { CanvasNodeState } from '../types';
/** Kind color per node type, as CSS tokens so every theme keeps its palette. */
export declare const KIND_COLOR: Record<CanvasNodeState['type'], string>;
